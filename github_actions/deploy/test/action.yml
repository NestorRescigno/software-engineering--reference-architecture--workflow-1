on:   
  workflow_call:
    # setting input
    inputs:
      # project name, eguals to product name example ancillaries
      project-name:
        type: string
        required: true
        default: "MyProyect"
jobs:
  work:
    runs:
      using: "composite"
      steps:
        - name: testing-run
          env:
            PROJECT_ENV: "hola"
          shell: bash
          run: echo "en la variable ${{env.PROJECT_ENV}} en el input " & echo ${{ inputs.project-name }}
