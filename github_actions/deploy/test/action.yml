on:   
  workflow_call:
    # setting input
    inputs:
      # project name, eguals to product name example ancillaries
      project_name:
        type: string
        required: true
        default: "MyProyect"       
jobs:
  work:
    steps:
      - name: testing-run
        run: |
          echo "en la variable en el input ${{ inputs.project_name }} "
