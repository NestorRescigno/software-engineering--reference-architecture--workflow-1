on:   
  workflow_call:
    # setting input
    inputs:
      # project name, eguals to product name example ancillaries
      project_name:
        type: string
        required: true
        default: "MyProyect"
runs:
   usign: 'composite'
   steps:
    - run: echo "project_name = ${{ inputs.project_name }}" >> $GITHUB_ENV
jobs:
  work:
    steps:
      - name: testing-run
        run: |
          echo "en la variable en el input ${{ env.project_name }} "
