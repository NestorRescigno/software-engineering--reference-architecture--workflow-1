on:   
  workflow_call:
    # setting input
    inputs:
      # project name, eguals to product name example ancillaries
      project-name:
        type: string
        required: true
        default: "MyProyect"

runs:
  using: "composite"
  env:
    PROJECT_ENV: "no le gusta" 
  steps:
    - name: testing-run
      shell: bash
      run: |
            echo "en la variable $PROJECT_ENV en el input " & echo ${{ inputs.project-name }}
